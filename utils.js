
/*const fs = require('fs')


// Obtener string y parsear a JSON
const appendLog = (entry) => {
  fs.appendFile('server.log', entry, (err) => {
    if (err) console.log(`No se ha podido usar el fichero`)
  })
}

const logTxt = (now, met, url) => {
  var entryLog=`Time: ${now} ${met} ${url} \n`
  console.log(entryLog)
}

module.exports = {
  appendLog,
  logTxt
}*/